# Calculadora Factor IPSC (PWA)

## Publicació a GitHub Pages

1. Puja `index.html`, `manifest.json` i `sw.js` al repositori.
2. Settings → Pages → Deploy from `main` i `/ (root)`.
3. Obre la URL al mòbil i prem `Afegeix a la pantalla d'inici`.
